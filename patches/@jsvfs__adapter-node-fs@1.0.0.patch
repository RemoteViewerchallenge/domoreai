diff --git a/index.ts b/index.ts
index 232f992..8872c08 100644
  public async write (path: string, contents: Buffer): Promise<void> {
    try {
      await this.mkdir(dirname(path), { recursive: true })
      // The type of contents is Buffer, which is a Uint8Array.
      // This is a valid type for writeFile, but the dependency's types are incorrect.
      await writeFile(join(this.root, path), contents as Uint8Array)
    } catch (err) {
      if (this.flush) {
        this.pending.push({
              contents: await readFile(join(this.root, newPath))
            }]
            break
    // @ts-ignore - Silencing incorrect type error from dependency
          case entry.isSymbolicLink():
            yield [newPath, {
              type: 'softlink',
    } catch (err) {
      if (this.flush) {
        this.pending.push({ type: 'commit', path })
        if (err instanceof Error) {
          this.events.emit('error', err)
        }
      }
    }
  }

