<p>he System Architect (Infrastructure Role)</p><p>Multi-Use Purpose: Manages the structural integrity of the codebase and enforces design patterns.</p><p></p><p>Refactor Mission: Executes the "Headless Structural Shift." It will refactor registry.tsx to separate implementation from prop definitions.</p><p></p><p>Hurdle Mitigation: It ensures that moving primitives to standalone files doesn't break the ComponentMap fast-lookup logic.</p><p></p><p>2. The UI/UX Technologist (Design System Role)</p><p>Multi-Use Purpose: Bridging the gap between the NebulaNode schema and the actual React render output.</p><p></p><p>Refactor Mission: Implements the Named Slot logic in NebulaRenderer.tsx and ensures StyleTokens are applied consistently.</p><p></p><p>Hurdle Mitigation: It addresses the recursive rendering complexity by ensuring slot-based children are resolved correctly from node IDs.</p><p></p><p>3. The Governance Warden (Security &amp; Tooling Role)</p><p>Multi-Use Purpose: Manages visibility, tool permissions, and schema validation across the platform.</p><p></p><p>Refactor Mission: Cleans the sidebar by implementing isHeadless and hidden filters in PropertyPanel.tsx.</p><p></p><p>Hurdle Mitigation: It replaces manual handleChange logic with a dynamic, schema-driven form generator.</p><p></p><p>The Grand Orchestrator Delegation Prompt</p><p>Use this prompt to activate your current VolcanoAgent and McpOrchestrator infrastructure:</p><p></p><p>ORCHESTRATOR COMMAND: Initiate UI Evolution Swarm</p><p></p><p>Task 1: System Architect</p><p></p><p>Analyze apps/ui/src/nebula/registry.tsx.</p><p></p><p>Refactor ComponentManifest to remove inline JSX.</p><p></p><p>Standardize the propSchema to use Zod-compatible definitions.</p><p></p><p>Task 2: UI/UX Technologist</p><p></p><p>Update apps/ui/src/features/nebula-renderer/NebulaRenderer.tsx to handle meta.slots.</p><p></p><p>Ensure children are passed to named slots (e.g., header, sidebar) based on the NebulaNode configuration.</p><p></p><p>Task 3: Governance Warden</p><p></p><p>Update apps/ui/src/components/nebula/system/PropertyPanel.tsx.</p><p></p><p>Implement visibility filtering based on isHeadless and hidden flags.</p><p></p><p>Replace manual input mappings with an automated form builder driven by the registry's propSchema.</p><p></p><p>Protocol: All agents must use filesystem-mcp for hot-reloading and validate changes against the central NebulaNode type.</p>