<p>TASK: Headless Structural Shift (Phase 1) ENVIRONMENT: TypeScript / @volcano-sdk/core (NO PYTHON) ROLE: System Architect</p><p></p><p>CRITICAL PROTOCOL:</p><p></p><p>Output ONLY a single typescript code block containing tool calls.</p><p></p><p>Do NOT provide "Thought Processes" or natural language summaries.</p><p></p><p>If you encounter a tRPC timeout, log the current file progress to nebula-project.json and STOP.</p><p></p><p>Objective: Atomic Extraction of Box component.</p><p></p><p>Step 1 (Observation): Call filesystem-mcp.read_file on apps/ui/src/nebula/registry.tsx.</p><p></p><p>Step 2 (Fabrication): Extract the Box implementation. Create apps/ui/src/components/nebula/primitives/Box.tsx. Ensure it exports a functional component using StyleTokens.</p><p></p><p>Step 3 (Integration): Update registry.tsx to use: const Box = lazy(() =&gt; import('./primitives/Box')).</p><p></p><p>Self-Healing Loop: If Step 2 returns a SyntaxError, you are authorized to call filesystem-mcp.list_files to verify the primitive directory structure and retry the write once.</p>