ENCRYPTION_KEY=
DATABASE_URL=

# Database Safety: Set to 'true' only for authorized admin operations (GDPR deletions, cleanup)
# Default: false (safe mode - prevents destructive operations)
APP_ALLOW_DESTRUCTIVE_DB=false
