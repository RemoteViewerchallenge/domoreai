Max turns reached. Last response: ```typescript
// Discover existing roles using 'system.role_registry_list'
const roles = await system.role_registry_list({});

// Evolve a new Role Variant (DNA) with specialized personality and toolsets
await system.role_variant_evolve({
  intent: {
    name: "Headless Swarm",
    description: "Specializes in meta-evolution and AST transformation",
    domain: "Backend",
    complexity: "HIGH",
    capabilities: ["GIT", "UI_ARCHITECT", "AST_TRANSFORMER"],
  },
});

// Surgically update the role's basePrompt or tools
await system.role_config_patch({
  roleId: "cl_HeadlessSwarm",
  updates: {
    basePrompt: "Create a Sentinel agent with GIT and UI_ARCHITECT capabilities",
    tools: ["read_file", "write_file", "terminal_execute", "ui_architect_tree_inspect"],
  },
});
```