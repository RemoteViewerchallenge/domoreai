# Agent templates

Prompt templates for COC; templates use simple mustache-like placeholders.
Templates are versioned by filename; when you change a template, bump the filename or add a version tag in the file.

IMPORTANT: This folder contains MOCK templates by default. Look for the word "MOCK" in the files to find mock content to remove when switching to production templates.